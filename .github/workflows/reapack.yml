name: ReaPack Index
on: [push]
jobs:
  index:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: cfillion/reapack-index@master
        with:
          token: ${{ secrets.GITHUB_TOKEN }}